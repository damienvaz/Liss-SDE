program Sets {

declarations

	a =7 -> integer;
	d = true, flag, flag2, bool   -> boolean;
	e, f = { x | x > 7}, g = {x |((x < 8) || (x > 15)) && (x < 130) } -> set;
	s1 -> sequence;
	v1 -> array size 2;


statements

// sets
	e = f ++ g;
	writeln(e);
	f = g ** f;
	g = g ** { x | x > 6 }; //interseccao

	flag = a in e;
	flag2 = a in g;
	bool = 8 in {x | (x < 10) && (x > 7) };
	d = 3 in g;
	s1 = <<1,2>>;
	v1 = [1,2];
}
